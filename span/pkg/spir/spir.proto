syntax = "proto3";
package spir;

option go_package = "span/pkg/spir";

// Stub message definitions
message TranslationUnit {
    string name = 1;
    repeated Function functions = 2;
}

message Function {
    string name = 1;
    // More fields will be added later
} 